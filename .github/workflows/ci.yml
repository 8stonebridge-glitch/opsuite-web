name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  aes-verify:
    name: aes-verify
    uses: 8stonebridge-glitch/aes-platform/.github/workflows/aes-verify.yml@main
    with:
      node-version: '20'
      install-command: npm ci
      verify-command: npm run verify
    secrets: inherit
