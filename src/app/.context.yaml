version: 1
last_updated: 2026-03-19T15:51:07.007Z
fingerprint: 69f9e4b7
scope: src/app
summary: Source directory.
maintenance: |-
  If you modify files in this directory, update this .context.yaml to reflect
  your changes. Update the summary, and any decisions or constraints that changed.
  Do NOT update the fingerprint manually — run `context rehash` or it will be
  updated automatically on the next `context status` check.
  If you only read files in this directory, do not modify this file.
  Do not include secrets, API keys, passwords, or PII in this file.
subdirectories:
  - name: admin/
    summary: Contains 2 source files
  - name: api/
    summary: Contains 0 source files
  - name: employee/
    summary: Contains 2 source files
  - name: onboarding/
    summary: Contains 1 source files
  - name: sign-in/
    summary: Contains 0 source files
  - name: sign-up/
    summary: Contains 0 source files
  - name: subadmin/
    summary: Contains 2 source files
exports:
  - function RootError(
  - function GlobalError(
  - "metadata: Metadata"
  - viewport
  - function RootLayout(
  - function RootLoading()
  - function NotFound()
  - async function HomePage()
  - function Providers(
dependencies:
  internal:
    - ./providers
imports:
  - path: ./globals.css
    symbols:
      - (side-effect)
  - path: ./providers
    symbols:
      - Providers
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - exports
  - dependencies.internal
  - imports
  - subdirectories
